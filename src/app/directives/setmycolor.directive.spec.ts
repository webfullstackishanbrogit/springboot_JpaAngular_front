import { SetmycolorDirective } from './setmycolor.directive';

describe('SetmycolorDirective', () => {
  it('should create an instance', () => {
    const directive = new SetmycolorDirective();
    expect(directive).toBeTruthy();
  });
});
